# Training service dependencies — kept deliberately minimal.
# No FastAPI, no LangChain, no database drivers.
# In production this image would also pin mlflow and boto3 (for S3 weights).
numpy==2.0.2
pandas==2.2.3
scikit-learn==1.5.2
joblib==1.4.2
